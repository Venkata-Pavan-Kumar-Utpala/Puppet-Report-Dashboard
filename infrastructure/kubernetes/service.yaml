apiVersion: v1
kind: Service
metadata:
  name: puppet-report-dashboard-svc
  namespace: devops
  labels:
    app: puppet-report-dashboard
spec:
  selector:
    app: puppet-report-dashboard
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
